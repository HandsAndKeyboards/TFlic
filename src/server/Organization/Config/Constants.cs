using System.IO;

namespace TFlic;

public static class Constants
{
    public static string configDir = Directory.GetCurrentDirectory();
    public static string configName = "config.json";
    public static string DbTarget = "DbConnectionString";
}